@model WebApp.Areas.Authorized.ViewModels.UserSubscriptionCreateVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserSubscription</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group">
                <label asp-for="UserSubscription.SubscriptionId" class="control-label">Subscription</label>
                <select asp-for="UserSubscription.SubscriptionId" class ="form-control" asp-items="Model.SubscriptionSelectList">
                    <option value="wrongData">--- Select Subscription ----</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
